name: Make SDK (Linux)
run-name: Make SDK ${{ inputs.version }}
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Platform Tools Version"
        required: true
        default: "36.0.2"
      tag:
        description: "Android Source Tag"
        required: true
        default: "android-16.0.0_r4"

permissions:
  contents: write
  packages: read

jobs:
  make:
    uses: ./.github/workflows/make-sdk.yml
    secrets: inherit
    with:
      platform: linux
      version: ${{ inputs.version }}
      tag: ${{ inputs.tag }}
      # Not built: powerpc64le-linux-gnu, x86_64-linux-gnux32
      # Dropped — all mips/mips64 (LLVM backend ICE: "failed to perform tail call
      #   elimination on a call site marked musttail").
      # Dropped — loongarch64-linux-muslf32: not a libc zig 0.15.1 ships
      #   (the available loongarch64 libcs are gnu/gnusf/musl/muslsf only).
      targets: >-
        ["aarch64-linux-musl", "aarch64-linux-gnu", "aarch64_be-linux-gnu",
         "aarch64_be-linux-musl", "arm-linux-gnueabi", "arm-linux-gnueabihf",
         "arm-linux-musleabi", "arm-linux-musleabihf", "armeb-linux-gnueabi",
         "armeb-linux-gnueabihf", "armeb-linux-musleabi", "armeb-linux-musleabihf",
         "hexagon-linux-musl", "loongarch64-linux-gnu", "loongarch64-linux-musl",
         "loongarch64-linux-gnusf", "loongarch64-linux-muslsf",
         "powerpc-linux-musleabi", "powerpc-linux-musleabihf",
         "powerpc64-linux-musl", "powerpc64le-linux-musl",
         "riscv32-linux-gnu", "riscv32-linux-musl",
         "riscv64-linux-musl", "riscv64-linux-gnu", "s390x-linux-gnu",
         "s390x-linux-musl", "thumb-linux-musleabi", "thumb-linux-musleabihf",
         "thumbeb-linux-musleabi", "thumbeb-linux-musleabihf",
         "x86-linux-gnu", "x86-linux-musl", "x86_64-linux-musl",
         "x86_64-linux-muslx32", "x86_64-linux-gnu"]
