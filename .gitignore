# fetch-source.sh: the release manifest and the AOSP checkouts
.manifest
/src
# build.sh: generated projects + build trees, deps, NDK, staged tools
/build
/extrabuild
/out
/android-ndk-*
/ndk.zip
# make-sdk.sh: the spliced SDK and its archive
/android-sdk
/android-sdk-*.tar.xz
/android-sdk-*.7z
# termux-adb / adb mDNS: cargo build artifacts + per-build crate cache
patches/termux/libtermuxadb/target
.cargo
__pycache__/
# remove git history
git_remove_history.sh
